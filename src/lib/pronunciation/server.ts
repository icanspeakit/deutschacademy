// The pronunciation provider the server uses to issue browser credentials. Server-only.
// Switch together with the re-export in ./index.ts.
export { azureServer as pronunciationServer } from "./azure.server";
