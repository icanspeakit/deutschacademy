---
// /jetzt — the current shape of the grammar topic page, on its own URL so it can be
// judged full-screen rather than in half a split view. Counterpart: /v1.
// Internal; not linked from the public nav.
import Layout from "../layouts/Layout.astro";
import TopicDemo from "../components/demo/TopicDemo.astro";
import "../styles/practice.css";
import "../styles/verben-praepositionen.css";
---
<Layout title="Jetzt – Grammatik-Layout" description="Interner Prototyp: die aktuelle Grammatik-Seite." noindex>
  <TopicDemo variant="jetzt" />
</Layout>
